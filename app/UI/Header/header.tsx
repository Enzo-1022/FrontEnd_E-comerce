import {anton} from "@/app/UI/font";

export default function Header() {
    return(
        <header>
            <h1 className={`${anton.className} anton-regular`}>Simas Turbo</h1>
        </header>
    )
}
