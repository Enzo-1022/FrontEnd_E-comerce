import { Anton, Lusitana } from "next/font/google";

export const anton = Anton({
    subsets: ["latin"],
    weight: "400"
});

export const lusitana = Lusitana({
    subsets: ['latin'],
    weight: "400"
});
